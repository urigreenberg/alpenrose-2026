# alpenrose-2026

אפליקציית טיול אחת (PWA), וניל JavaScript. אין framework, אין build step, אין
package.json — הקבצים שבריפו הם בדיוק מה שמוגש ב-GitHub Pages.

## שחרור גרסה — לעשות תמיד, בלי לבקש אישור

כשמסיימים שינוי, משחררים אותו. בעל הריפו ביקש מפורשות לא לעצור לאישור.
הרצף, בסדר הזה:

1. להריץ את **כל** חבילות האימות ולתקן עד שהן ירוקות
2. קומיט לענף העבודה (`claude/...`) ודחיפה
3. להעלות את `APP_BUILD` ב-`app.js` — הפורמט `YYYY-MM-DD.N`, המונה מתאפס כל יום
4. למזג ל-`main` (`git merge --no-ff`)
5. **להריץ את כל החבילות שוב על העץ הממוזג**, לפני הדחיפה ולא אחריה
6. `git push origin main` — זה מה שפורס ל-GitHub Pages
7. לחזור לענף העבודה

"לא צריך אישור כי אני לא בודק את זה" פירושו שהבדיקות הן השער היחיד, לא ששום
שער לא נחוץ. לכן שלב 5 אינו אופציונלי: מיזוג יכול לשבור דברים שהענף לבדו לא
שבר, ואין מי שיתפוס את זה אחרי הדחיפה.

**לא להעלות את `CACHE` ב-`sw.js`.** ה-Service Worker מגיש את המעטפת
stale-while-revalidate ומזהה שינוי לפי `etag`/`last-modified`, ונתוני הטיול
ממילא network-first — קוד חדש מגיע למכשירים בלי החלפת שם מטמון.

## מלכודת: טיוטה מקומית גוברת על מה שפורסם

`loadTrip()` מעדיף את `tp:<tripId>:draft` מ-localStorage על פני
`trips/<id>/trip.json`, וזה מכוון. המשמעות: מי שיש לו טיוטה שמורה **לא יראה
שום שינוי תוכן שפרסמנו** עד שישליך אותה. זה כבר הפיל 12 תמונות שנראו "חסרות".
פס `#draftBar` בראש הדף מסמן את המצב ומציע השלכה. **בכל שחרור שנוגע ב-trip.json
— להזכיר את זה למשתמש בתשובה.**

שדות ישנים בטיוטות שמורות מטופלים ב-`migrateInfoLinks()` (למרות השם, הוא מטפל
גם ב-`parking`). כשמוציאים שדה משימוש — להוסיף שם מחיקה, אחרת טיוטה ישנה תמשיך
להצביע על המצב הישן בשקט.

## מודל הנתונים והקישורים

- קוד ונתונים מופרדים: כל טיול הוא `trips/<id>/trip.json`, ו-`trips/index.json`
  מונה אותם.
- **ניווט תמיד לפי שם, אף פעם לא לפי קואורדינטה.** קואורדינטה שגויה נכשלת
  בשקט (נקודת החניה של אייבזה ישבה 1.4 ק"מ מהחניה האמיתית); שם אפשר לאמת מול
  האתר הרשמי של המקום. כל מחרוזת ניווט כוללת יישוב — הטיול על גבול
  אוסטריה-גרמניה ושמות חוזרים על עצמם.
- `mapsQuery` = המקום עצמו (צ'יפ מפה + חיפוש גוגל). `parkingQuery` = יעד
  הנהיגה, רק כשהוא **אינו** המקום עצמו, ואז **חובה** `parkingNote` שמסביר לאן,
  כמה עולה, ואיך ממשיכים משם. `edSaveForm` חוסם שמירה בלעדיו.
- המחקר ליעדי ניווט הוא **סטטי ונעשה פעם אחת** ונכתב ל-`trip.json`. אין לוגיקת
  ניחוש בזמן ריצה. מקום חדש = מחקר חדש, ידנית.

## שתי תוכניות: `days` ו-`altPlan`

`days` הוא המסלול המקורי; `altPlan.days` הוא מסלול חלופי (אותן פעילויות בסדר
אחר, לפי מזג האוויר). `DAYS` נגזר מ-`PLAN_ID` ב-`planDays()`, וזו נקודת החיבור
**היחידה** — המסלול, "עכשיו" ונקודות התחזית כולם קוראים את `DAYS`, אז המתג
מחליף את כולם יחד. כך זה חייב להישאר: "עכשיו" שמראה תוכנית אחרת מזו שנבחרה הוא
שקר בדיוק ברגע שסומכים עליו.

- **`WX.cacheKey` חייב לכלול את מזהה התוכנית** (`wxCacheKey()`). לכל תוכנית סט
  נקודות אחר, ומטמון של אחת שמוגש לשנייה נראה כמו "אין תחזית" בלי שום הסבר.
- **העורך עורך את `days` בלבד.** `altPlan` שורד כי העורך משכפל את כל אובייקט
  הטיול, אבל שינוי במסלול המקורי לא מחלחל לחלופה — צריך לערוך אותה ידנית בקובץ.
- בדיקות המבנה על `trip.json` חייבות לרוץ על **שתי** התוכניות, לא רק על `days`.

## אימות

אין framework בדיקות. הבדיקות הן סקריפטים חד-פעמיים בתיקיית ה-scratchpad, ולא
בריפו. הדפוס שעובד:

- `node -c app.js editor.js` + `python3 -c json.load` על `trip.json`
- סקריפט Python שאוכף חוקים על `trip.json` (אין `0.0 ק"מ`, אין `ll=`, כל
  `parkingQuery` עם `parkingNote`, כל בלוק מסתיים אחרי שהתחיל)
- Playwright מול `python3 -m http.server`, שאוסף `href`-ים מהדף המרונדר ובודק
  אותם — לא בודקים את הנתונים בלבד, בודקים מה באמת יוצא למסך
- לפני עריכה מתוכנתת של `trip.json`: לוודא ש-`json.dumps(obj, ensure_ascii=
  False, indent=2) + "\n"` זהה בית-בבית לקובץ, כדי שה-diff יישאר מינימלי

כלים: Playwright ב-`/opt/node22/lib/node_modules/playwright`, Chromium
ב-`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`.

## מגבלות הסביבה

אין גישת רשת יוצאת — כל דומיין חיצוני חסום בפרוקסי (`EGRESS_BLOCKED`), כולל
`WebFetch`. הכלי היחיד שעובד הוא `WebSearch`. כלומר אפשר לאמת עובדות מול
תקצירי חיפוש, אבל אי אפשר למשוך אתר רשמי, להריץ geocoder, או לבדוק את האתר החי
ב-GitHub Pages. כשמשהו לא ניתן לאימות — לומר את זה, לא לנחש.

## כתיבה

הממשק והתוכן בעברית, RTL. הערות בקוד בעברית. הודעות קומיט באנגלית.
